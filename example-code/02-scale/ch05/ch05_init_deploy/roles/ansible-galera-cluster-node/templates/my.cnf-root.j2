# {{ ansible_managed }}

[client]
user=root
password="{{galera_root_password}}"

[mysql]
user=root
password="{{galera_root_password}}"

[mysqldump]
user=root
password="{{galera_root_password}}"

[mysqldiff]
user=root
password="{{galera_root_password}}"
